/**
 * Supabase client placeholder.
 *
 * The scaffold runs entirely on mock data and local state. When production
 * wiring begins:
 *
 *   1. `npx expo install @supabase/supabase-js`
 *   2. Add EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY env vars.
 *   3. Export a real client from this file:
 *        export const supabase = createClient(url, anonKey, { ... });
 *   4. Replace the mock reads/writes in AppDataContext and AuthContext with
 *      Supabase queries, and mirror the permission helpers as RLS policies.
 *
 * TODO: wire to Supabase.
 */
export const supabase = null;
