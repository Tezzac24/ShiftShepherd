/**
 * Supabase client placeholder.
 *
 * The scaffold runs entirely on mock data and local state. The backend
 * foundation already exists — see supabase/migrations/ (schema + RLS),
 * supabase/seed/ (dev data), and docs/supabase-integration-plan.md for the
 * step-by-step wiring order. When wiring begins:
 *
 *   1. `npx expo install @supabase/supabase-js`
 *   2. Copy .env.example to .env and fill in
 *      EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY.
 *   3. Export a real client from this file:
 *        export const supabase = createClient(url, anonKey, { ... });
 *   4. Replace mock reads/writes feature by feature per the integration plan
 *      (AuthContext first, then announcements as the first vertical slice).
 *
 * TODO: wire to Supabase.
 */
export const supabase = null;
